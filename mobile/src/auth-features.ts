// Native passkeys need the Associated Domains entitlement, which the paid
// Apple Developer Program team (C9P58ZP4AQ) can sign. Keep this in step with
// `associatedDomains` in app.config.ts.
export const NATIVE_PASSKEYS_ENABLED = true;
