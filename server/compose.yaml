services:
  daily-mirror:
    build: .
    ports:
      - "3000:3000"
    environment:
      DAILY_MIRROR_UPLOAD_TOKEN: ${DAILY_MIRROR_UPLOAD_TOKEN:-local-dev}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
